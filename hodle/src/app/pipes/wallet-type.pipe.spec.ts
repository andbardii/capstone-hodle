import { WalletTypePipe } from './wallet-type.pipe';

describe('WalletTypePipe', () => {
  it('create an instance', () => {
    const pipe = new WalletTypePipe();
    expect(pipe).toBeTruthy();
  });
});
