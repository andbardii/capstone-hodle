import { TogpercentPipe } from './togpercent.pipe';

describe('TogpercentPipe', () => {
  it('create an instance', () => {
    const pipe = new TogpercentPipe();
    expect(pipe).toBeTruthy();
  });
});
