import { TogsithomePipe } from './togsithome.pipe';

describe('TogsithomePipe', () => {
  it('create an instance', () => {
    const pipe = new TogsithomePipe();
    expect(pipe).toBeTruthy();
  });
});
