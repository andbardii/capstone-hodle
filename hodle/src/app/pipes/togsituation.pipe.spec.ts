import { TogsituationPipe } from './togsituation.pipe';

describe('TogsituationPipe', () => {
  it('create an instance', () => {
    const pipe = new TogsituationPipe();
    expect(pipe).toBeTruthy();
  });
});
